# 📊 Data Analysis Web App (Streamlit)

An interactive data analysis app built with Python and Streamlit.  
Upload your CSV file, explore, clean, visualize, and download the cleaned data — all in one place!

## 🚀 Features
- Upload and preview CSV files
- View dataset info (columns, datatypes, shape)
- Perform data cleaning (remove duplicates/nulls)
- Visualize data (line, bar, area, scatter, correlation plots)
- Download cleaned dataset

## 🛠️ Installation
```bash
pip install -r requirements.txt
streamlit run app.py
